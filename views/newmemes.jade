.row
    .meme(style="background-image: url('" + meme.src + "')")

    form(method="POST")
        label(for=meme.id)
        input(type="text" id=meme.id name=meme.id)
        button(type="submit" value="Submit new message")

    each message in messages
        if message.id == meme.id
            label= message.text